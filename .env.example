# PostgreSQL 連線字串（部署到 Render 時，去 Render 的 Postgres 服務複製 Internal/External Database URL）
DATABASE_URL=postgresql://user:password@host:5432/dbname

# NiceGUI 用來加密登入 session 的密鑰，正式環境務必換成隨機字串
STORAGE_SECRET=change-this-to-a-random-secret

# 服務啟動的 port（Render 會自動注入 PORT，本機測試預設 8080）
PORT=8080
